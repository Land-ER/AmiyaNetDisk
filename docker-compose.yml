version: '3.8'

services:
  app:
    build: .
    ports:
      - "5000:5000"
    volumes:
      - ./uploads:/app/uploads
      - ./app.db:/app/app.db
    env_file:
      - .env
    restart: unless-stopped
