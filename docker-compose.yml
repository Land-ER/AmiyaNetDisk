version: '3.8'

services:
  app:
    build:
      context: .
      args:
        BUILD_EMBEDDING: "1"
    ports:
      - "5000:5000"
    volumes:
      - ./uploads:/app/uploads
      - ./app.db:/app/app.db
    env_file:
      - .env
    restart: unless-stopped
